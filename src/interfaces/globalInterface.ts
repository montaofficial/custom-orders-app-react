export default interface GlobalStyle {
  theme: {
    colors: {
      navbar: string;
      background: string;
      backgroundSecondary: string;
      container: string;
      fill: string;
      textPrimary: string;
      textSecondary: string;
      shadow: string;
    };
    mobile: string;
  };
}
